export type StoryCard = {
  org: string;
  title: string;
  meta: string;
  summary: string;
  challenge: string;
  approach: string;
  impact: string;
};

export type SoftSkill = {
  title: string;
  body: string;
};
